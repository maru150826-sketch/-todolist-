# 集中ダッシュボード 改善レポート

## 今回直したこと

- 画面上部に「今日のスタート」を追加しました。
  - 次にやるタスクを自動表示
  - 今日の集中時間と完了数を表示
  - 通常スタートと「2分だけ始める」を用意
- 既に実装されていたバックアップ・復元機能を、押せるボタンとして表示しました。
- `file://` で直接開いた時に Service Worker 登録エラーを出さないようにしました。
- カレンダーに「集中画面へ」ボタンを追加し、単体表示と埋め込み表示の両方に対応しました。
- カレンダー上部の開発者向け更新案内を削除しました。

## 見つかった重要な問題

### 1. クラウド同期の利用者IDが固定

`index.html` と `log.html` は、Supabase の利用者IDに `default-user` を使っています。
複数人が同じ公開アプリを使うと、設定次第では学習データが混ざる可能性があります。

修正方針:

1. Supabase Authで匿名ログインまたはメールログインを追加する
2. データ行の `user_id` に認証済みユーザーIDを保存する
3. Row Level Securityで「自分の行だけ読める・書ける」規則を設定する
4. 現在の `default-user` データを新しいユーザーへ一度だけ移行する

### 2. 外部CDNが読めないと同期機能が使えない

SupabaseライブラリをCDNから取得しています。オフライン時やCDN障害時は、ローカル保存は使えても同期できません。

修正方針:

- 同期が利用できない時は「ローカル保存のみ」と明示する
- 本格運用ではライブラリをアプリと一緒に配布する

### 3. 1ファイルに機能が集中している

`index.html` は約6,000行あり、表示、タイマー、タスク、同期、分析が同居しています。
小さな変更でも別機能への影響を確認しにくい状態です。

修正方針:

- `storage.js`、`timer.js`、`tasks.js`、`sync.js`、`ui.js` に段階的に分ける
- 最初に日付計算、時間集計、タスク並び順を自動テスト可能な関数として分離する

## 次に試すと良いアイデア

- 「今日の最低ライン」を1つだけ設定し、達成後は明確に終了扱いにする
- 前回の続きから再開できる「昨日の続き」ボタン
- 授業資料やGitHub課題を登録し、タスクに関連ファイル・URLを紐づける
- エラー文を貼ると「原因・確認箇所・試す修正・テスト」を記録できるデバッグノート
- 週末に「できたこと3つ」を自動抽出する週次レビュー
- 集中時間だけでなく、提出物やテスト結果など成果も記録する

## テスト項目

- タスクがない時、開始ボタンが誤動作しない
- タスクがある時、通常開始と2分開始が正しい時間で動く
- 一時停止、再開、画面を最小化した後も残り時間が正しい
- 手動ログ、完了タスク、振り返りが再読み込み後も残る
- バックアップしたJSONを復元できる
- カレンダーで日付、月移動、週集計が正しく表示される
- 本体からカレンダーへ移動し、集中画面へ戻れる
- スマートフォン幅でボタンや入力欄が画面外にはみ出さない
- オフラインでもローカル保存とタイマーが使える
- 認証導入後、別ユーザーのデータを読めない

## 今回の確認結果

- 初期表示: 正常
- 「2分だけ始める」: `02:00` で開始し、一時停止表示へ変化
- バックアップボタン: 表示を確認
- 本体からカレンダーへの移動: 正常
- カレンダーから本体への復帰: 正常
- 直接ファイルを開いた時のService Workerエラー: 解消

